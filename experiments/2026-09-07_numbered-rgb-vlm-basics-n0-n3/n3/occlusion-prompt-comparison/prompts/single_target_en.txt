Examine the numbered RGB image.

The following IDs are confirmed to be the complete and correct set of visible
object-marker IDs:

{visible_ids_json}

Use the physical object identified by ID {target_id} as the single reference target.

For every other confirmed ID, determine whether its associated physical object is
closer to the camera than the target or farther from the camera than the target.

Use the following labels:

- FRONT_OF_TARGET: the other object is closer to the camera than the target object.
- BEHIND_TARGET: the other object is farther from the camera than the target object.

Compare every other object directly with the same target object.
Do not order or compare the non-target objects with one another.

Use the following visual evidence:

1. If an object overlaps the target, the physical object that visually occludes the
   other physical object is closer to the camera.
2. If an object does not overlap the target, compare where their physical bodies touch
   the shelf. All objects stand on the same flat shelf. The object whose shelf-contact
   point appears lower in the image is closer to the camera.
3. The red numbered marker only identifies an object. Do not use the marker's position
   or size as evidence of depth.
4. Do not use apparent object size alone because the objects have different physical sizes.

In visible_ids, return the complete provided ID list in its original order.
In target_id, return the provided reference target ID.

Return every non-target ID exactly once in relations_to_target. Do not include the
target ID itself in relations_to_target.

Return only one valid JSON object with this structure:

{
  "visible_ids": [],
  "target_id": 0,
  "relations_to_target": [
    {
      "id": 0,
      "relation": "FRONT_OF_TARGET"
    }
  ]
}

Do not output a total front-to-back order, explanations, Markdown, confidence scores,
coordinates, object descriptions, or additional fields.
